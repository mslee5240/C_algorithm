#include <stdio.h>

int main() {
    // 여기에 코드를 작성해주세요.
    int nArr[10] = { 0 };
    int max_500 = 0, min_500 = 0;
    for (int i = 0; i < 10; ++i) {
        scanf("%d", &nArr[i]);
        if (nArr[i] < 500 && nArr[i] > min_500) {
            min_50 = nArr[i];
        }
    
    }
    return 0;
}